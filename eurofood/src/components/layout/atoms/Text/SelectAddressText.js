import styled from 'styled-components/native';

const SelectAddressText = styled.Text`
    font-size: 16px;
    padding-top: 8px;
    padding-bottom: 8px;
    color: ${({ theme }) => theme.colors.orange(1)};
`;

export default SelectAddressText;
