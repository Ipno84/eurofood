import styled from 'styled-components/native';

const FixedBottom = styled.View`
    position: absolute;
    bottom: 20px;
    left: 0;
    right: 0;
    align-items: center;
`;

export default FixedBottom;
