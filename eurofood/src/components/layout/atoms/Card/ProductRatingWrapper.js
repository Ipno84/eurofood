import styled from 'styled-components/native';

const ProductRatingWrapper = styled.View`
    align-items: center;
    justify-content: center;
    padding-bottom: 8px;
    flex: 1;
    flex-direction: row;
`;

export default ProductRatingWrapper;
