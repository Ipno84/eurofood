import Wrapper from './../Wrapper';
import styled from 'styled-components/native';

const PromoWrapper = styled(Wrapper)`
    margin-top: 18px;
    margin-left: 8px;
    margin-right: 8px;
    margin-bottom: 8px;
    border-radius: 2px;
`;

export default PromoWrapper;
