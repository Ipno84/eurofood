import Wrapper from './../../atoms/Card/Wrapper';
import styled from 'styled-components/native';

const LoginBlock = styled(Wrapper)`
    border-radius: 0;
    margin-top: 16px;
`;

export default LoginBlock;
